# Review: chore/bump-deps → main

This branch bumps four Rust dependencies in `Cargo.toml` and refreshes three of them in `Cargo.lock`. No source file changed, so there is no API surface, logic, or convention to review — the only thing worth your attention is the one semver-breaking bump.

**Coverage.** Nothing ran, and that's the correct result rather than a gap. Scope detection over `f451521..4df4a5b` (1 commit, clean working tree) found 2 changed files — `Cargo.toml` (manifest) and `Cargo.lock` (lockfile) — and **0 changed lines in source files**. All four aspects were inactive for that reason, none at your request:

- `wire_api` — no wire contract, route, or CLI changes.
- `code_api` — no exported surface changed.
- `logic` — 0 changed lines in source/wire files (threshold 25).
- `convention` — no new code; conventions are only audited on new code.

No review agents were dispatched and no findings were verified, so nothing below carries a verification verdict.

## Worth knowing before you merge

- **`reqwest` 0.12.4 → 0.13.0 is a breaking bump.** For a `0.x` crate, Cargo treats the minor position as the major, so `0.12 → 0.13` is a semver-incompatible release with no compatibility guarantee. It's the only bump here that can break you. Nothing in `src/` imports `reqwest` today (`grep` over `src/main.rs` and `src/config.rs` finds no reference), so the blast radius is a build check rather than a code migration — but that also means a green build proves less than usual here, since the new version is never exercised.
- The other three are compatible upgrades: `serde` 1.0.203 → 1.0.210, `tokio` 1.38.0 → 1.41.1, `tracing` 0.1.40 → 0.1.41.
- **The manifest and lockfile moved different sets.** `Cargo.toml` bumps four crates; `Cargo.lock` records only three — it has no `tracing` entry at all, before or after. The next `cargo build` will rewrite the lock, so the committed lock isn't the one you'll ship. Worth confirming the lock was regenerated by Cargo rather than hand-edited.

That's the whole review. There is no code here to find bugs in, and I'd rather say so than pad the report.
